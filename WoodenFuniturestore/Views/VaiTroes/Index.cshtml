@model IEnumerable<WoodenFuniturestore.Models.VaiTro>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenVaiTro)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenVaiTro)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VaiTroId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VaiTroId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VaiTroId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
